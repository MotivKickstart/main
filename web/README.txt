to run locally:

install docker / docker desktop
open the web folder in cmd (the folder with the docker-compose.yml file)
run: docker compose up
if theres an error exit with ctrl-c and run the command again